# FPL Wrapped Environment Variables

# =============================================================================
# ELASTICSEARCH (OPTIONAL - EXPERIMENTAL)
# =============================================================================
# These features are in trial mode and can be enabled/disabled independently
# Set ENABLE_ELASTICSEARCH=false to disable all ES features instantly

# Master switch for Elasticsearch
ENABLE_ELASTICSEARCH=false

# Elasticsearch Cloud connection
# Get from: https://cloud.elastic.co/deployments
ELASTICSEARCH_URL=https://your-deployment.es.region.cloud.es.io:443
ELASTICSEARCH_API_KEY=your-api-key-here

# Elastic Agent Builder ID for chat feature
# Create agent in Kibana (Search → Agent Builder) and note the agent ID
ELASTIC_AGENT_ID=elastic-ai-agent

# Index name for gameweek decisions (can customize)
ELASTICSEARCH_INDEX_NAME=fpl-gameweek-decisions

# SSL verification (set to 'false' if on corporate network with SSL inspection)
# WARNING: Only use false in development! Keep true in production
ELASTICSEARCH_REJECT_UNAUTHORIZED=true

# =============================================================================
# CORPORATE NETWORK SETTINGS
# =============================================================================
# If you're on a corporate laptop with SSL inspection, you may need to disable
# SSL certificate verification for Node.js. This affects both FPL API and ES.
# WARNING: Only use in development! Never in production!

# Uncomment this line if you get "unable to get local issuer certificate" errors:
# NODE_TLS_REJECT_UNAUTHORIZED=0

# =============================================================================
# FEATURE FLAGS (Only work when ENABLE_ELASTICSEARCH=true)
# =============================================================================

# Enable mini-league gameweek reports
# Allows analyzing decisions across all managers in a league
ENABLE_MINI_LEAGUE_REPORTS=false

# Enable chat interface with FPL data
# Requires ELASTIC_AGENT_ID to be configured (Elastic Agent Builder)
ENABLE_CHAT=false

# =============================================================================
# FPL API (No changes needed - already working)
# =============================================================================
# FPL API requires no authentication, these are just for reference
# FPL_API_BASE=https://fantasy.premierleague.com/api

TELEGRAM_BOT_TOKEN=your_token_here
